# /etc/cron.d/gb-website — install on mycelium as root
SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin

# Nightly at 03:17
17 3 * * * root /opt/gb-website/repo/scripts/backup.sh >> /var/log/gb-website-backup.log 2>&1
